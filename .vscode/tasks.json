{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run C++ with I/O",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "g++ \"${file}\" -o \"${fileBasenameNoExtension}.exe\" -std=c++17 -O2 && \"${fileBasenameNoExtension}.exe\" < input.txt > output.txt"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
